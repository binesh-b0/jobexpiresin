import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "JobExpiresIn - AI Job Replacement Predictions",
  description: `JobExpiresIn is a web application that uses AI to predict when various job roles might be replaced by artificial intelligence. 
  Enter a job title to discover similar roles, key responsibilities, and receive a timeline for potential AI replacement. The project provides valuable insights 
  into the future of work in an increasingly automated world.`,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
